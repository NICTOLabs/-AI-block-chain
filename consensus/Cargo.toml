[package]
name = "consensus"
version = "0.1.0"
edition = "2021"
description = "Core consensus primitives for the AI-native blockchain"
license = "MIT"

[dependencies]
thiserror = "2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha3 = "0.10"
