[package]
name = "tender-sdk"
version = "0.1.0"
edition = "2021"
description = "Rust SDK for the Tender AI-native blockchain"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
